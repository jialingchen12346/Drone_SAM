# 阶段性总结与整改（2026-03-24 14:07）

- 本轮状态：训练已结束，自动闭环结果已整理完毕。
- 新增完成轮次（batch=8）：`gen_ohem_dice_cmr060_aux005_no_photo`
  - `val mIoU=66.71`
  - `test mIoU=64.93`
  - `gap=1.78`
- 排名变化：当前 leaderboard 第 2，仍低于最优 `65.32`（`gen_ohem_only_cmr060_aux010`）。

## 关键整改

1. 修复训练启动器异常退出
- 问题：训练完成后触发 `train_exit_code=127`（导致 auto-loop 误记 failed）。
- 处理：`scripts/run_ddp_train.sh` 改为数组命令执行，避免续行解析风险。
- 结果：已补跑评测并回填 history/leaderboard。

2. 防止心跳空转反复拉起
- 问题：全部 recipe 完成后，旧逻辑仍会自动恢复调度器。
- 处理：心跳新增 `pending_recipe_cnt` 门控，`0` 时不再 auto-recover。
- 当前：`train_cnt=0, loop_cnt=0, pending_recipe_cnt=0`。

3. 磁盘可用空间稳定
- 当前 `/`：`20G used / 11G avail (66%)`。
- 已采用历史轮次瘦身策略（保留 `best.pth`，删除非活跃 `epoch_30.pth`）。

## 下一步（执行建议）

1. 固化“吞吐配置保留、泛化策略回退”：保留 `batch=8`，回到 `OHEM-only` 主线。 
2. 启动新一轮消融：`no_photo_distort + detail_aux_weight 0.03~0.05`。
3. 目标：冲击 `test mIoU > 65.32`，并维持 `gap <= 2.5`。
