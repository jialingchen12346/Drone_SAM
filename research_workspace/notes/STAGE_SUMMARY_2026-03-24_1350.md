# 阶段性总结与改进（2026-03-24 13:50）

## 一、阶段结论（已完成）

- 自动闭环（训练→评测→反馈）已完整跑完 3 轮。
- 结果概览（FMB test mIoU）：
  1. `gen_ohem_dice_cmr060_aux010`: `64.62`
  2. `gen_ohem_only_cmr060_aux010`: `65.32`（当前最佳）
  3. `gen_ohem_dice_cmr055_aux008_blur030`: `64.58`
- 关键观察：
  - 第二轮（去掉 dice 的 ablation）在 test 上最好，说明当前泛化并非“增强越强越好”。
  - 第三轮 strong_aug 配置虽提升 val 到 `69.61`，但 test 仅 `64.58`，`gap=5.03`，出现过拟合/分布偏移风险。

## 二、工程状态（已完成）

- 远程自动调度、自动评测、leaderboard 汇总已跑通。
- 心跳监管已上线（本机 crontab 每 5 分钟巡检）。
- 心跳已接入低盘自动清理，避免训练中断。

## 三、问题定位

1. 泛化仍未突破：
- test mIoU 仍在 `64.5~65.3` 区间波动。

2. 吞吐尚有优化空间：
- 2x5090 的显存占用约 `7~8GB/卡`，GPU 利用率多在 `40~70%`。
- 根因更接近“单步吞吐配置”，而不是 epoch 设置本身。

3. 磁盘风险持续存在：
- 多轮训练后 checkpoint 累积快，需持续执行保守留档策略。

## 四、已落地整改

- `auto_loop_config.json` 已升级为：
  - `batch-size=8`
  - `num-workers=12`
  - `--no-ddp-find-unused-params`
  - `save-freq=30`
- 说明：当前进行中的轮次保持旧参数，下一次新启动轮次自动应用。

## 五、下一阶段动作（立即执行）

1. 运行 1 轮 batch=8 验证实验：
- 核验吞吐、GPU 利用率、每 epoch 耗时、训练稳定性。

2. 同步评测并比较：
- 与当前最优配置（`gen_ohem_only_cmr060_aux010`）对比 test mIoU 与 gap。

3. 若吞吐提升但精度下降：
- 优先调 `cat-max-ratio / detail-aux-weight / blur-prob`，不引入更复杂结构。

4. 若吞吐与精度均改善：
- 固化为新主配置，推进到论文实验主表。

## 六、最新进展（2026-03-24 13:50 之后）

- 已按整改参数启动新一轮自动实验：
  - recipe: `gen_ohem_dice_cmr060_aux005_no_photo`
  - 关键参数：`batch-size=8`、`num-workers=12`、`--no-ddp-find-unused-params`
- 在线观测（采样时刻）显示：
  - 显存占用约 `20.9GB/卡`（相较先前约 `7~8GB/卡` 明显提高）
  - GPU 利用率采样达到约 `86% / 64%`
- 结论：吞吐参数整改已实际生效，后续继续跟踪 epoch 耗时与最终 test mIoU。
