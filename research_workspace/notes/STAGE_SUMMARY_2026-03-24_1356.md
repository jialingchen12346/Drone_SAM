# 阶段性总结与整改（2026-03-24 13:56）

## 1) 当前阶段结论

- 自动闭环已完成 3 轮，当前最优仍是：`gen_ohem_only_cmr060_aux010`（test mIoU=`65.32`）。
- `batch-size=8` 吞吐整改已经落地并在新轮次生效：
  - 运行 recipe：`gen_ohem_dice_cmr060_aux005_no_photo`
  - 关键参数：`batch-size=8`、`num-workers=12`、`--no-ddp-find-unused-params`
  - 在线观测：GPU 利用率采样约 `44~100%`，显存约 `20.9GB/卡`。

## 2) 本阶段问题与定位

- 泛化问题仍在：历史 test mIoU 仍主要在 `64.5~65.3` 区间。
- 磁盘风险持续：auto-loop 每轮双 checkpoint 占用高，系统盘会快速逼近阈值。
- 监控误报来源：训练 stdout 缓冲会导致心跳出现“log stale”告警。

## 3) 已执行整改（已落地）

- 日志实时化：
  - `scripts/run_ddp_train.sh` 默认导出 `PYTHONUNBUFFERED=1`
  - `scripts/auto_train_eval_loop.py` 默认补齐 `PYTHONUNBUFFERED=1`
- 心跳监控增强：
  - `scripts/heartbeat_remote_supervisor.sh` 新增 `active_work_dir`
  - 训练活跃度判断新增 checkpoint mtime 兜底，降低 stale 误报
- 磁盘二次清理（在线执行，未中断训练）：
  - 对已完成 auto-loop 轮次删除 `epoch_30.pth`，保留每轮 `best.pth`
  - `/` 可用空间 `7.6G -> 12G`（`75% -> 61%`）

## 4) 下一步执行

1. 等待当前 `batch=8` 轮次结束并自动评测，回填 history/leaderboard。
2. 与最优轮次（65.32）做同口径对比：若 test 未提升，优先调 `cat-max-ratio/detail-aux-weight`，不新增结构复杂度。
3. 继续执行“低盘自动清理 + 心跳自愈”以确保闭环持续运行。
